function validateForm()
{
	// postal code
	var str = document.forms["form_address"]["postal"].value
	if (str.length == 6)
	{
		//var patt = new RegExp("\w\d\w\d\w\d")
		//if (patt.test(str) == false)
		{
			//alert("Invalid Canadian Postal Code. Wrong sequence of characters.");
			//return false;
		}
	}
	else if (str.length != 0)
	{
		alert("Invalid Canadian Postal Code. Wrong length.");
		return false;
	}
}
